# Адрес бэкенда для прокси Vite в режиме разработки (npm run dev).
# В продакшене (Docker + Nginx) не используется — проксированием занимается Nginx.
VITE_BACKEND_URL=http://localhost:8000

# Базовый путь REST API (по умолчанию /api/v1). Менять обычно не нужно.
# VITE_API_BASE=/api/v1
